# Architecture

ProjectForge is structured into:

- `core`: engines (template, upgrade, monorepo)
- `domain`: models, services, rules
- `infra`: VS Code integration, workspace, git
- `templates`: stack-specific templates
- `ui`: wizard, panels, messages
- `utils`: shared helpers
