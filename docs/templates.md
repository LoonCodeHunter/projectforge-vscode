# Templates

Each template is a TypeScript module exporting a `TemplateDescriptor`:

```ts
export const reactTsAdvanced: TemplateDescriptor = {
  id: "react-ts-advanced",
  name: "React TypeScript Advanced",
  category: "web",
  files: [...],
  folders: [...],
  extras: [...]
};
